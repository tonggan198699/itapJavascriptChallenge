'use strict';

import axios from 'axios';

export function printSomething(something){
  return new Promise((resolve, reject) => {
    if(something == 'trem'){
      reject("deu ruim");
    } else {
      resolve(something);
    }
  });
}